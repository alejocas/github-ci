name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run a one-line scirpt
      run: echo Hello world!

    - name: Run a multi-line script
      run: |
        echo Add other actions to build, test,
        echo and deploy your project.